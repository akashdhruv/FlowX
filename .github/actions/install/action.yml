# Name of the composite action
name: install

inputs:
  image: 
    description: 'Input image for flowx'
    required: true
 
runs:
  using: "composite"
  steps:
    - run: maple container run --commit --image=${{inputs.image}} "./setup develop && ./setup install && ./setup clean"
      shell: bash
